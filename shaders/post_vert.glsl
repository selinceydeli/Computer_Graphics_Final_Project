#version 410

// Per-vertex attributes
// in vec2 pos;
// in vec2 texCoords;

// // Data to pass to fragment shader
// out vec2 fragPos;
// out vec2 fragCoords;
in vec3 pos;
in vec3 normal;

uniform mat4 mvp;

void main() {
    // fragPos = pos;
    // fragCoords = texCoords;
}